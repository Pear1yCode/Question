<div id="headerid" class="maincls"></div>
// headerid에 대한 노드를 $header라는 변수로 구하고
// maincls에 대해 CSS 선택자를 이용해 $main이라는 변수로 노드 취득을 해보시오.

const $header = document.getElementById('header'); // id 취득

const $main = document.querySelector('.maincls'); // class(css 선택자) 취득
